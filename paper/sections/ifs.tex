% Owner: Sreeja

This insert is reserved for the final IBD and IFS prose. It should define
Intent-Behavior Discrepancy as the semantic mismatch between declared workload
intent and observed runtime behavior, then explain IFS as the alignment signal
used to quantify that mismatch without turning the section into a long formula
derivation.
